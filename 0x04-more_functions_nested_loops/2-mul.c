#include "main.h"

/**
 * mul - multiplt 2 numbers
 * @a: number 1
 * @b: number 2
 * Return: a * b
 */
int mul(int a, int b)
{
	int r;

	/*for (i = 0; i < 9; i++)
		_putchar(i);
	/* print a new line at the end */
	r = a * b;
	return r;
}
