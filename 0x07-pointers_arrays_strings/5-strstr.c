#include "main.h"

/**
 * _strstr - concat string.
 * @haystack: string pointer
 * @needle: string
 * Return:  pointer
 */
char *_strstr(char *haystack, char *needle)
{
	unsigned int i;

	for (i = 0; i < n; i++)
		s[i] = b;


	return (s);
}
