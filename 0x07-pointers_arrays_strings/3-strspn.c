#include "main.h"

/**
 * _strspn - concat string.
 * @s: string pointer
 * @accept: filling char
 * Return:  unsigned int 
 */
char *_strspn(char *s, char *accept)
{
	unsigned int i;

	for (i = 0; i < n; i++)
		s[i] = b;


	return (s);
}
