#include "main.h"

/**
 * _strpbrk - concat string.
 * @s: string pointer
 * @b: filling char
 * @n: int length
 * Return:  pointer
 */
char *_strpbrk(char *s, char accept)
{
	unsigned int i;

	for (i = 0; i < n; i++)
		s[i] = b;


	return (s);
}
