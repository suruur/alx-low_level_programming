#include "main.h"

/**
 * print_chessboard - concat string.
 * @a: string pointer
 * Return:  pointer
 */
void print_chess_board(char (*a)[8])
{
	unsigned int i;

	for (i = 0; i < n; i++)
		s[i] = b;


	return (s);
}
