#include "main.h"

/**
 * _mecpy - concat string.
 * @dest: string pointer
 * @src: filling char
 * @n: unsigned int length
 * Return:  pointer
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{
	unsigned int i;

	for (i = 0; i < n; i++)
		s[i] = b;


	return (s);
}
