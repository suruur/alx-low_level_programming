#include "main.h"

/**
 * _strchr - concat string.
 * @s: string pointer
 * @c: filling char
 * Return:  pointer
 */
char *_strchr(char *s, char c)
{
	unsigned int i;

	for (i = 0; i < n; i++)
		s[i] = b;


	return (s);
}
